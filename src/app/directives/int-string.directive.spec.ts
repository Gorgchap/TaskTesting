import { IntStringDirective } from './int-string.directive';

describe('IntStringDirective', () => {
  it('should create an instance', () => {
    const directive = new IntStringDirective();
    expect(directive).toBeTruthy();
  });
});
